enum HeadLinePosition { LEFT, RIGHT, CENTER }

enum SelectTypes {
  DOCUMENT_TYPE,
  CUSTOMER_TYPE,
  DISTRICT,
  SELLER,
}

enum SelectTypesProduct {
  ITEM_TYPE,
  UNIT_TYPE,
  CURRENCY_TYPE,
  SALE_AFFECTATION_IGV_TYPE,
  PURCHASE_AFFECTATION_IGV_TYPE,
}

enum DownloadType { TCK, A4 }

enum NullifyType { VOIDED, SUMMARY }
